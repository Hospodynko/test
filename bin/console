#!/usr/bin/env php
<?php
require_once __DIR__ . '/../vendor/autoload.php';

use App\Commands\RunScript;
use Symfony\Component\Console\Application;

$app = new Application();
$app->add(new RunScript());

$app->run();
